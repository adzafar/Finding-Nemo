<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.flipkart.adeelzafar.finding_nemo.RedLevelAlert"
    android:background="@color/Red">

    <LinearLayout style="@style/AlertScreens">
        <TextView
            android:text="R"
            android:id="@+id/textView"
            android:textColor="@color/Red"
            style="@style/BannerCharacter"
        />

        <LinearLayout
           style="@style/AlertListsContainer">

            <ScrollView
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_weight="1">

                <LinearLayout
                    style="@style/TagView"
                    android:id="@+id/red_upper">

                </LinearLayout>
            </ScrollView>

            <View style="@style/Divider"/>

            <ScrollView
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_weight="1">
                <LinearLayout
                    style="@style/TagView"
                    android:id="@+id/red_lower">

                </LinearLayout>
            </ScrollView>

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
