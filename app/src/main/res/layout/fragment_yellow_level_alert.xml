<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.flipkart.adeelzafar.finding_nemo.YellowLevelAlert"
    android:background="@color/Yellow">

    <LinearLayout style="@style/AlertScreens">
        <TextView
            android:text="Y"
            android:id="@+id/textView"
            android:textColor="@color/Yellow"
            style="@style/BannerCharacter"
        />

        <LinearLayout
            style="@style/AlertListsContainer">

            <ScrollView
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_weight="1">
                <LinearLayout
                    style="@style/TagView"
                    android:id="@+id/yellow_upper">
                </LinearLayout>
            </ScrollView>

            <View style="@style/Divider"/>

            <ScrollView
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_weight="1">
                <LinearLayout
                    style="@style/TagView"
                    android:id="@+id/yellow_lower">

                </LinearLayout>
            </ScrollView>
        </LinearLayout>

    </LinearLayout>
</FrameLayout>
